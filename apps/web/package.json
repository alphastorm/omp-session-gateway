{
  "name": "@omp-session-gateway/web",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "dependencies": {
    "@omp-session-gateway/protocol": "workspace:*"
  },
  "scripts": {
    "build": "bun ../../scripts/build-web.ts",
    "typecheck": "bunx tsc --noEmit && bunx tsc -p tsconfig.worker.json --noEmit"
  }
}
